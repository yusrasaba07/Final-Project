import React, { useState } from "react"; import "./App.css"; import AboutUs from "./AboutUs"; import ProductList from "./ProductList"; import CartItem from "./CartItem"; function App() { const [showProductList, setShowProductList] = useState(false); const [currentPage, setCurrentPage] = useState("landing"); const handleGetStarted = () => { setShowProductList(true); setCurrentPage("products"); }; const navigateTo = (pageName) => { if (pageName === "landing") { setShowProductList(false); } setCurrentPage(pageName); }; return ( <div className="app-container" data-repo="e-plantShopping"> {currentPage === "landing" && !showProductList && ( <div className="landing-page" style={{ position: "relative", height: "100vh", display: "flex", flexDirection: "column", justifyContent: "center", alignItems: "center", color: "white" }}> <div className="background-image"></div> <h1 className="landing-title">Paradise Nursery</h1> <div style={{ borderBottom: "2px solid white", width: "100px", margin: "10px auto" }}></div> <AboutUs /> <button className="get-started-btn" onClick={handleGetStarted}>Get Started</button> </div> )} {currentPage === "products" && showProductList && ( <ProductList useNavigate={navigateTo} /> )} {currentPage === "cart" && ( <CartItem useNavigate={navigateTo} /> )} </div> ); } export default App;
